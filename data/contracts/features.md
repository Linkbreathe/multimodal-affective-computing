# Feature contract

Windows are exactly 10 seconds and exist only as feature time axes.  Condition
training consumes aggregations of those windows.  New run outputs keep
preprocessed data and features inside `artifacts/runs/<run_id>/`.
