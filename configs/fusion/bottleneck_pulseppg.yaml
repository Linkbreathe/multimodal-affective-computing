fusion_type: bottleneck
fusion:
  d_common: 256
  bottleneck:
    bottleneck_ratio: 0.5
    dropout: 0.1
modalities:
  ppg:
    encoder: pulseppg
    embed_dim: 512
    enabled: true
training:
  lr: 0.0001
  max_epochs: 100
