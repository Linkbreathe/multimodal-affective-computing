"""SEED-V benchmark material.

This package contains dataset-specific runners, configurations and tests.
Reusable encoder and fusion implementations remain in :mod:`mac` because
some of them are used by the RELAX pipeline.
"""
