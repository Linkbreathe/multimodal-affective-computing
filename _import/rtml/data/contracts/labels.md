# Label contract

Each participant--Condition row has exactly two supervised targets:
`relaxation` and `discomfort`, normalized to 0--1.  The outer split is leave
one participant out.  Other questionnaire fields can be retained for audit but
are not replacements for these targets.
