# Auxiliary materials

This directory contains research planning documents and historical review notes. They provide context and provenance for analyses, but they are not required to install the package, run the core feature pipeline, execute tests, or start the Unity services.

The executable source remains under `src/`, `analysis/`, and `integrations/`; configuration and environment entry points remain at the repository root and under `configs/`.
